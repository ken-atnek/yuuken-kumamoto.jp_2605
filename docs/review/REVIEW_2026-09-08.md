# 公開前最終チェックレポート

- 実施日: 2026-09-08
- 対象: 株式会社 雄建 Webサイト
- 公開予定URL: `https://yuuken-kumamoto.jp/`
- 対象構成: Next.js App Router / Static Export / TypeScript / SCSS

## 進捗サマリー

| 状態 | No. | 項目                         | メモ                  |
| ---- | --: | ---------------------------- | --------------------- |
| ⏳   |   1 | 問い合わせフォーム           | 本番受信先へ変更済み  |
| ⏳   |   2 | ビルド成果物                 | ユーザー側でbuild確認 |
| ✅   |   3 | canonical                    | 対応済み              |
| ✅   |   4 | `/works/` のOGP URL          | 対応済み              |
| ⏳   |   5 | 現行URLとリダイレクト        | サーバー側で手動管理  |
| ✅   |   6 | Apple Touch Icon             | 指定削除済み          |
| ✅   |   7 | メイン画像                   | 正式画像へ変更済み    |
| ✅   |   8 | メタディスクリプション       | 表記修正済み          |
| ✅   |   9 | 施工事例JSONの文字正規化     | 対応済み              |
| ✅   |  10 | 施工事例JSON取得失敗時の表示 | 対応済み              |
| ✅   |  11 | モーダルのアクセシビリティ   | 対応済み              |
| ✅   |  12 | 開発用チェックコマンド       | lint修正済み          |
| ✅   |  13 | 不要ファイル                 | `.DS_Store` 削除済み  |

## 現在の判定

要再確認。

主要ページの表示と本番・デモビルドは確認済み。ただし、チェック後に問い合わせPHPの配置、ビルドスクリプト、SEOメタ情報、施工事例JSONまわりを変更したため、最新版での本番ビルドと公開サーバー上でのフォーム送信確認が必要。

## 公開前に必須確認

### 1. 問い合わせフォーム

ステータス: ⏳ 再確認待ち

- PHPを `public/backend/contact.php` へ移動済み
- フォーム送信先を `/backend/contact.php` に変更済み
- ビルド後は `out/backend/contact.php` にコピーされる想定
- 本番サーバーの `/backend/contact.php` にPHP設置済み
- 直接アクセスで `{"success":false,"error":"無効なリクエストです。"}` が返ることを確認済み
- 本番PHPへ直接POSTすると `{"success":true,"message":"お問い合わせを送信しました。"}` が返ることを確認済み
- 最新 `out` の再アップロード後、本番画面からのフォーム送信が成功することを確認済み
- 現在の受信先は本番用の `info@yuuken-kumamoto.jp`
- テスト用受信先 `ken.atnek@gmail.com` はコメントアウトして保持
- 送信元は `info@yuuken-kumamoto.jp`
- 更新後のPHPを本番サーバーへ反映し、必要に応じて本番受信先で最終確認する

対象ファイル:

- `public/backend/contact.php`
- `src/components/Top/ContainerContact.tsx`

### 2. ビルド成果物

ステータス: ⏳ 再確認待ち

- `build:demo` / `build:prod` の完了後に `out/db` を削除するよう変更済み
- `public/db` の原本は削除されない
- 通常の `npm run build` はローカル確認用として変更していない
- 変更後の本番ビルドは途中で進行が止まったため未完了
- 今後のビルド確認はAI側では実行せず、必要時にユーザー側で実行する
- 次回ビルド後に以下を確認する
  - `out/backend/contact.php` が存在する
  - `out/db` が存在しない
  - `out/404.html` と `out/404` が存在しない
- FTPのミラーリング削除で、公開サーバー上の既存 `/db` を削除しないよう注意する

対象ファイル:

- `package.json`
- `public/backend/contact.php`

## SEO関連

### 3. canonical

ステータス: ✅ 対応済み

- トップページと `/works/` に本番時のみcanonicalを出力する設定を追加済み

対象ファイル:

- `src/app/page.tsx`
- `src/app/works/page.tsx`

### 4. `/works/` のOGP URL

ステータス: ✅ 対応済み

- `/works/` 側でページ固有の `og:url` を設定済み

対象ファイル:

- `src/app/layout.tsx`
- `src/app/works/page.tsx`

### 5. 現行URLとリダイレクト

ステータス: ⏳ 再確認待ち

- `http://yuuken-kumamoto.jp/` は `https://yuuken-kumamoto.jp/` へ301
- `https://www.yuuken-kumamoto.jp/` は `https://yuuken-kumamoto.jp/` へ301
- `https://yuuken-kumamoto.jp/works/` は200
- `https://yuuken-kumamoto.jp/sitemap.xml` は200、`/` と `/works/` を出力
- `https://yuuken-kumamoto.jp/robots.txt` は `Allow: /`
- `/contact/`、`/about/`、`/company/`、`/works.html`、`/index.php` は404
- `2605` フォルダは `https://yuuken-kumamoto.jp/` の公開ルートとしてサーバー側で設定済み
- `.htaccess` はマニュアルで設定・管理する
- `https://yuuken-kumamoto.jp/index.html` は200のため、必要ならサーバー側で `/` へ301
- `https://yuuken-kumamoto.jp/works` や `/2605/` 配下の扱いは、必要に応じてサーバー側 `.htaccess` で調整する

参照ファイル:

- `docs/seo/SEO_SETUP.md`

## コンテンツ・画像

### 6. Apple Touch Icon

ステータス: ✅ 対応済み

- 実ファイルがないため、`layout.tsx` の Apple Touch Icon 指定を削除済み

対象ファイル:

- `src/app/layout.tsx`
- `public/favicon/`

### 7. メイン画像

ステータス: ✅ 対応済み

- トップ画像の参照を `/images/hero.webp` に変更済み
- `_dummy` 配下の画像は使用しない方針

対象ファイル:

- `src/components/Top/ContainerHero.tsx`
- `public/images/hero.webp`

### 8. メタディスクリプションの表記

ステータス: ✅ 対応済み

- 「タイル・ブロック工事」に修正済み
- 「内装仕上工事など」に修正済み

対象ファイル:

- `src/app/page.tsx`

### 9. 施工事例JSONの文字正規化

ステータス: ✅ 対応済み

以下の2件をNFC正規化済み。

- `エムグランデ:飲食店`
- `ラウンジ中村:飲食店`

対象ファイル:

- `public/db/works/works.json`

## 動作・保守性

### 10. 施工事例JSON取得失敗時の表示

ステータス: ✅ 対応済み

- JSON取得時に `res.ok` の確認と `catch` を追加済み
- 取得失敗時は従来どおり施工事例部分を非表示にし、コンソールにエラーを出す

対象ファイル:

- `src/components/Top/ContainerWorks.tsx`
- `src/components/Works/WorksList.tsx`

### 11. モーダルのアクセシビリティ

ステータス: ✅ 対応済み

- 施工事例モーダルに `role="dialog"` / `aria-modal` / `aria-labelledby` を追加済み
- 施工事例モーダルの閉じるボタンに読み上げ用ラベルを追加済み
- 問い合わせ結果モーダルに `role="dialog"` / `aria-modal` / `aria-label` を追加済み
- 問い合わせ結果モーダルの閉じるボタンに読み上げ用ラベルを追加済み
- 両モーダルともEscキーで閉じる処理と、表示時に閉じるボタンへフォーカスする処理を追加済み

対象ファイル:

- `src/components/Works/WorksModal.tsx`
- `src/components/Modal.tsx`

### 12. 開発用チェックコマンド

ステータス: ✅ 対応済み

- `npm run lint` を `next lint` から `eslint .` に変更済み
- ESLint設定をNext.js 16向けの flat config 形式に変更済み
- `npm run lint` は警告なしで正常終了
- 対象ファイルのPrettierは正常終了
- Stylelintは正常終了確認済み

対象ファイル:

- `package.json`
- `eslint.config.mjs`
- `src/components/Top/ContainerContact.tsx`

### 13. 不要ファイル

ステータス: ✅ 対応済み

- `public` / `out` 配下の `.DS_Store` を削除済み
- `.gitignore` に `.DS_Store` は設定済み

## 確認済み項目

- `output: 'export'`
- `trailingSlash: true`
- `images: { unoptimized: true }`
- `public/db` はクライアントから `fetch()` している
- `cache: 'no-store'` を使用している
- `force-dynamic` は使用していない
- `robots.ts` / `sitemap.ts` に `force-static` がある
- 本番ビルド時のrobotsは `Allow: /`
- 本番sitemapには `/` と `/works/` が出力される
- デモビルド時は `noindex, nofollow`
- デモrobotsは `Disallow: /`
- デモsitemapは空
- OGP画像は1200×630px
- 施工事例JSONはJSON形式として正常
- 3カテゴリ・8事例・画像参照55件を確認
- 必須項目、ID重複、画像重複、画像パス、画像ファイルの存在に問題なし
- PC / スマートフォン表示を確認
- ハンバーガーメニューの開閉を確認
- 施工事例一覧と画像モーダルを確認
- 問い合わせフォームの必須項目エラー表示を確認
- 確認時のブラウザコンソールエラーなし
- Stylelint正常終了

## 公開直前チェック手順

1. ユーザー側で `npm run build:prod` を実行する
2. `out/backend/contact.php` が存在することを確認する
3. `out/db` が存在しないことを確認する
4. `out/robots.txt` と `out/sitemap.xml` を確認する
5. `out` を公開サーバーへ転送する
6. サーバー上の `/db` が残っていることを確認する
7. トップと `/works/` の表示を確認する
8. 問い合わせフォームからテスト送信する（確認済み）
9. `ken.atnek@gmail.com` で受信を確認する（確認済み）
10. 更新後の `public/backend/contact.php` を本番サーバーへ反映する
